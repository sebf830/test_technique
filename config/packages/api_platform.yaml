api_platform:
    show_webby: false

    enable_swagger: true
    enable_swagger_ui: true

    swagger:
        api_keys:
            apiKey:
                name: Authorization
                type: header
    mapping:
        paths: ["%kernel.project_dir%/src/Entity"]
